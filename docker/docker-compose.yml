version: '3.0'
services:
  school_db_container:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: lokation
    ports:
      - 3306:3306
    volumes:
      - ./mysql_db:/var/lib/mysql
